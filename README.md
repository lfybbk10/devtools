## CI Status

[![Java CI with Checkstyle and JaCoCo](https://github.com/lfybbk10/devtools/actions/workflows/ci.yml/badge.svg)](https://github.com/lfybbk10/devtools/actions/workflows/ci.yml)

## Code Review Checklist

Используйте этот чеклист для само-ревью перед запросом ревью у ментора:

### Функциональность
- [ ] Код решает поставленную задачу полностью
- [ ] Обработаны граничные случаи (null, пустые данные, экстремальные значения)
- [ ] Обработка ошибок реализована корректно

### Тесты
- [ ] Добавлены тесты для нового функционала (или обновлены существующие)
- [ ] Все тесты проходят локально: `./gradlew test`
- [ ] Покрыты позитивные и негативные сценарии
- [ ] JaCoCo coverage ≥ 80% для нового кода

### Читаемость и стиль
- [ ] Имена переменных, методов и классов отражают назначение
- [ ] Нет дублирования кода (DRY principle)
- [ ] Checkstyle проходит без ошибок: `./gradlew checkstyleMain`
- [ ] Нет закомментированного кода или отладочного вывода (`System.out.println`)

### Документация
- [ ] README обновлён (если добавлена новая функциональность)
- [ ] Публичные методы имеют JavaDoc (если применимо)
- [ ] Примеры использования актуальны
- [ ] Runbook обновлён (если изменились команды запуска/проверки)

### Производительность и безопасность
- [ ] Нет очевидных проблем производительности
- [ ] Нет хардкода паролей, токенов или конфиденциальных данных

## Примеры Code Review комментариев

### Хорошие комментарии (конструктивные)

**Пример 1:**

**Проблема:** Метод `calculateDiscount` (строка 45) имеет 3 вложенных if-else и 40 строк.
**Почему это важно:** Сложная логика плохо тестируется и тяжело поддерживается.
**Предложение:** Вынести каждое условие в отдельный метод (например, `isEligibleForBonusDiscount()`)
и использовать паттерн Strategy для разных типов скидок.



**Пример 2:**

**Проблема:** Тест `testProcessOrder` (строка 78) проверяет только успешный сценарий.
**Почему это важно:** Не проверена обработка ошибок при недостаточном балансе.
**Предложение:** Добавить тест `testProcessOrder_InsufficientBalance_ThrowsException()`
с использованием `assertThatThrownBy()`.



### Плохие комментарии (неконструктивные)

**Пример 1:**

Этот код ужасен, полностью переписать.


**Почему плохо:** Нет конкретики (что именно плохо), нет предложения (как исправить),
токсичный тон (демотивирует автора).

**Пример 2:**

Здесь лучше использовать Stream API.


**Почему плохо:** Нет объяснения почему лучше, нет примера как переписать,
неясно какую проблему это решает.

## Результаты само-ревью DVT-9

### Найденные проблемы

#### 1. Забыт отладочный вывод
**Файл:** src/main/java/ru/mentee/power/devtools/student/StudentList.java (строка 39)
**Проблема:** Оставлен `      System.out.println("Debug: studentAdd");`
**Почему важно:** Отладочный вывод замусоривает логи production-приложения и создаёт впечатление небрежности.
**Исправление:** Удалить строку или заменить на logger (если логирование настроено).


#### 2. Оставлен пустой TODO без контекста
**Файл:** src/main/java/ru/mentee/power/devtools/progress/ProgressTracker.java (строка 57)
**Проблема:** Оставлен пустой TODO
**Почему важно:** Пустой TODO без контекста и обозначения задачи засоряет код
**Исправление:** Удалить строку или дополнить.

#### 3. Закомментированный код
**Файл:** src/main/java/ru/mentee/power/devtools/progress/ProgressDemo.java (строка 24)
**Проблема:** Закомментирована 1 строка вывода
**Почему важно:** Закомментированный код создаёт путаницу: непонятно зачем он сохранён и актуален ли.
Если нужна история изменений — она в Git.
**Исправление:** Удалить закомментированный код. Если нужна старая версия — посмотреть в Git History.

## Quick Start

**Через Gradle Tool Window (IDEA):**  
`View → Tool Windows → Gradle` →  
`Tasks → application → run` — запуск  
`Tasks → build → build` — сборка  
`Tasks → verification → test` — тесты (двойной клик по задаче).

**Через Run Anything (Ctrl+Ctrl):**  
Нажмите `Ctrl` дважды → введите:

```bash
gradle run
gradle build
gradle test
```

---

### Packages

`ru.mentee.power` — основной пакет со всеми классами

---

### Поля

| Имя                  | Описание                                      |
|----------------------|-----------------------------------------------|
| `menteeName`         | Имя менти                                     |
| `sprintNumber`       | Номер спринта                                 |
| `plannedHoursPerWeek`| План часов в неделю                           |
| `readyForSprint()`   | Проверка, можно ли начинать спринт           |

## Правило веток: feature/DVT-X

В репозитории используются следующие правила именования веток:

- Основная ветка: `master`
- Фича-ветки: `feature/DVT-X`, где `X` — номер задачи или тикета.

Примеры:
- `master`
- `feature/DVT-3`

## Git локальный цикл: шаги и команды


```bash
git checkout master
git pull origin master
git checkout -b feature/DVT-3
git status
git add .
git commit -m "DVT-3: описание"
```

## Сценарий ручной проверки DVT-7

### Запуск приложения
1. Откройте Gradle Tool Window (View → Tool Windows → Gradle)
2. Выполните: devtools → Tasks → application → run
3. Ожидаемый вывод в Run Tool Window:
   Суммарно: пройдено 25 из 36 уроков, осталось 11 уроков

### Запуск тестов
1. Откройте Gradle Tool Window
2. Выполните: devtools → Tasks → verification → test
3. Ожидаемый вывод: BUILD SUCCESSFUL, все тесты зелёные

### Отладка через Debug
1. Установите breakpoint на строке цикла while в ProgressTracker.calculateProgress
2. Запустите Debug: кликните правой кнопкой на main → Debug 'ProgressTracker.main()'
3. Используйте Step Over (F8) для прохождения итераций
4. Проверьте Variables: counter, remainingHours должны изменяться корректно
5. Используйте Evaluate Expression (Alt+F8): вычислите remainingLessons * 2
6. Ожидаемый результат Evaluate: 14 (для completedLessons=5, totalLessons=12)

### Что делать при ошибках
- Если вывод некорректен: проверьте логику цикла через Debug
- Если тесты красные: откройте вывод теста, найдите AssertionError, скорректируйте метод
- Если Debug не останавливается: убедитесь, что breakpoint установлен (красный кружок)

## Кодстайл-гайд проекта devtools

Проект следует правилам Google Java Style Guide с адаптацией.
Автоматическая проверка: ./gradlew checkstyleMain

### 1. Именование методов: camelCase

До:    public void add_student(Student s) { }
После: public void addStudent(Student student) { }

Почему: Java Convention требует camelCase для методов.
Источник: https://google.github.io/styleguide/javaguide.html#s5.3-camel-case

### 2. Пробелы после if/for/while

До:    if(condition) {
После: if (condition) {

Почему: улучшает читаемость, отделяет ключевое слово от выражения.
Источник: Oracle Code Conventions — Whitespace

### 3. Длина строки: максимум 120 символов

До:    public List getStudentsFromSpecificCityWithVeryLongName...
После: public List getStudentsByCity(String city) {

Почему: длинные строки затрудняют чтение в редакторе и при code review.
Источник: https://google.github.io/styleguide/javaguide.html#s4.4-column-limit

### 4. Порядок импортов

До:    import java.util.List; import java.util.ArrayList; import java.io.File;
После: import java.io.File; import java.util.ArrayList; import java.util.List;

Почему: алфавитный порядок упрощает поиск импортов.
Источник: IntelliJ IDEA → Code → Optimize Imports

### 5. Фигурные скобки для if

До:    if (condition) doSomething();
После: if (condition) { doSomething(); }

Почему: скобки обязательны даже для однострочных блоков.
Источник: https://google.github.io/styleguide/javaguide.html#s4.1.1-braces-always-used

## Информационный поиск — результаты DVT-11

### Запросы и источники

| № | Запрос | Операторы                                                        | Официальный источник | Альтернатива | Статус | Дата проверки |
|---|--------|------------------------------------------------------------------|----------------------|--------------|--------|---------------|
| 1 | Lombok Gradle Short| site:search.maven.org "lombok" OR site:mvnrepository.com "lombok" |https://search.maven.org/artifact/org.projectlombok/lombok/1.18.30/jar | https://mvnrepository.com/artifact/org.projectlombok/lombok/1.18.30| 200 OK | 19.12.2025    |
| 2 |Java Stream API документация | site:docs.oracle.com "stream api" intitle:api java 21            | https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/stream/Stream.html|https://www.baeldung.com/java-8-streams | 200 OK |    19.12.2025           |
| 3 |горячие клавиши IntelliJ IDEA |site:www.jetbrains.com/help "keymap" "intellij idea"|https://www.jetbrains.com/help/idea/mastering-keyboard-shortcuts.html | встроенная справка IDE (Help → Keymap Reference)| 200 OK |   19.12.2025            |

### AI-промпты и сравнение ассистентов

#### Промпт 1: План решения

**Задача: Добавить плагин Checkstyle в проект**
**Контекст: проект на Gradle, Java 25**
**Ограничения: нет**
**Ожидаемый результат: пошаговый план как это сделать**
**Критерии успеха: плагин подключен, проверка checkstyleMain работает**

**AI-1 (ChatGPT):**

Ниже — рабочий пошаговый план, чтобы **подключить Checkstyle в Gradle-проект (Java 25)** и добиться, чтобы **`checkstyleMain` реально запускался и падал на нарушениях**.

---

## 1) Подключи плагин в `build.gradle`

### Groovy DSL (`build.gradle`)

```groovy
plugins {
    id 'java'
    id 'checkstyle'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(25)
    }
}

checkstyle {
    toolVersion = '10.21.2'   // стабильная ветка, обычно без сюрпризов
    // configFile зададим позже, когда создадим файл
}

tasks.withType(Checkstyle).configureEach {
    reports {
        xml.required = false
        html.required = true
    }
}
```

> Почему я не ставлю «самую новую любую» версию: иногда самые свежие релизы ломают совместимость конфигов/IDE. `10.21.2` — часто “безболезненный” выбор. Если хочешь строго «самое новое», можно, но тогда чаще всплывают нюансы.

---

## 2) Добавь конфиг Checkstyle в проект

Создай файл:

`config/checkstyle/checkstyle.xml`

Минимальный конфиг, который точно стартует:

```xml
<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">
    <module name="TreeWalker">
        <module name="LineLength">
            <property name="max" value="120"/>
        </module>

        <module name="UnusedImports"/>
    </module>
</module>
```

---

## 3) Укажи Gradle, где лежит конфиг

В `build.gradle` допиши/замени блок `checkstyle { ... }`:

```groovy
checkstyle {
    toolVersion = '10.21.2'
    configFile = file("$rootDir/config/checkstyle/checkstyle.xml")
}
```

---

## 4) Убедись, что структура исходников стандартная

Checkstyle по умолчанию проверяет:

* `src/main/java/...`
* `src/test/java/...`

Если у тебя не стандартная структура — скажи, подстроим.

---

## 5) Запусти проверку

Команды:

```bash
./gradlew checkstyleMain
```

или чтобы прогонялось вместе со всем:

```bash
./gradlew check
```

---

## 6) Критерий “checkstyleMain работает” — как проверить быстро

Сделай намеренное нарушение, например:

`src/main/java/.../Demo.java`

```java
import java.util.List; // не используется

public class Demo {
    public static void main(String[] args) {
        System.out.println("hi");
    }
}
```

Запускаешь:

```bash
./gradlew checkstyleMain
```

Ожидаемо:

* задача **упадёт** из-за `UnusedImports`
* html-отчёт будет тут:

   * `build/reports/checkstyle/main.html`

---

## 7) Частые причины “checkstyleMain не запускается / не находит файлы”

1. **Нет файла конфига** или путь неверный
   → проверь `configFile = file("$rootDir/config/checkstyle/checkstyle.xml")`

2. **Код не в `src/main/java`**
   → тогда надо настроить `sourceSets`

3. **Таска не падает на ошибках** (редко, но бывает при кастомной настройке)
   → можно явно включить:

   ```groovy
   tasks.withType(Checkstyle).configureEach {
       ignoreFailures = false
   }
   ```

---

Если хочешь — скинь свой текущий `build.gradle` (или `build.gradle.kts`) и структуру папок, и я точечно под тебя подгоню конфиг + настройки так, чтобы `checkstyleMain` гарантированно работал без сюрпризов.


**AI-2 (Яндекс):**

## Пошаговая инструкция по подключению Checkstyle в Gradle‑проект (Java)

### Шаг 1. Добавить зависимость плагина в `build.gradle`

Откройте корневой файл `build.gradle` вашего проекта и добавьте плагин в секцию `plugins`:

```groovy
plugins {
    id 'java'
    id 'checkstyle'
}
```

> **Примечание:** плагин `checkstyle` входит в стандартный набор Gradle, отдельная зависимость не требуется.

### Шаг 2. Настроить параметры Checkstyle

Добавьте конфигурацию плагина в тот же файл `build.gradle`:

```groovy
checkstyle {
    configFile = file('config/checkstyle/checkstyle.xml')
    toolVersion = '10.17.1'  // актуальная версия на момент 2025 г.
    ignoreFailures = false
    showViolations = true
    maxWarnings = 0
    maxErrors = 0
}
```

**Пояснения к параметрам:**
- `configFile` — путь к файлу конфигурации Checkstyle (будет создан на следующем шаге).
- `toolVersion` — версия Checkstyle (проверьте актуальную на [mvnrepository.com](https://mvnrepository.com)).
- `ignoreFailures` — `false` означает, что сборка прервётся при нарушениях.
- `maxWarnings`/`maxErrors` — пороги для прерывания сборки.

### Шаг 3. Создать файл конфигурации Checkstyle

1. Создайте директорию:
   ```
   mkdir -p config/checkstyle
   ```

2. Создайте файл `checkstyle.xml` в этой директории. Пример минимального содержимого:

```xml
<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">
<module name="Checker">
    <module name="TreeWalker">
        <module name="EmptyStatement"/>
        <module name="EqualsHashCode"/>
        <module name="IllegalInstantiation"/>
        <module name="MissingSwitchDefault"/>
        <module name="OneStatementPerLine"/>
        <module name="UnnecessaryParentheses"/>
    </module>
</module>
```

> **Совет:** Для полноценной конфигурации возьмите шаблон с [официального сайта Checkstyle](https://checkstyle.org/config.html) или из репозитория `checkstyle/checkstyle`.

### Шаг 4. Проверить работу плагина

1. Выполните команду в терминале:
   ```bash
   ./gradlew checkstyleMain
   ```

2. Ожидаемый результат:
   - Если нарушений нет: вывод `BUILD SUCCESSFUL`.
   - Если есть нарушения: список ошибок в консоли и `BUILD FAILED`.

### Шаг 5. Интегрировать в процесс сборки

Чтобы проверка запускалась автоматически при сборке, добавьте зависимость в `build.gradle`:

```groovy
tasks.named('check') {
    dependsOn 'checkstyleMain'
}
```

Теперь `./gradlew check` будет включать проверку Checkstyle.

### Шаг 6. Настроить IDE (опционально)

Для удобства разработки:
- **IntelliJ IDEA:** установите плагин *Checkstyle-IDEA* и укажите путь к `checkstyle.xml`.
- **Eclipse:** используйте плагин *Checkstyle* с аналогичной настройкой.

### Проверка критериев успеха

1. **Плагин подключен:** в `build.gradle` есть секция `plugins { id 'checkstyle' }`.
2. **Проверка работает:** команда `./gradlew checkstyleMain` выполняется без ошибок конфигурации.
3. **Нарушения обнаруживаются:** при внесении умышленного нарушения (например, пустой оператор `;`) сборка падает с указанием строки.

### Возможные проблемы и решения

- **Ошибка «Could not find config file»**  
  Проверьте путь в `configFile` и существование файла.
- **Сборка не прерывается при ошибках**  
  Убедитесь, что `ignoreFailures = false` и `maxErrors > 0`.
- **Неактуальная версия Checkstyle**  
  Обновите `toolVersion` в конфигурации.

**Сравнение:**

Оба ответа совпали в базовом подходе: подключение плагина checkstyle через plugins, создание файла checkstyle.xml, указание configFile и проверка через ./gradlew checkstyleMain. Также в обоих вариантах проверка завязана на стандартную структуру src/main/java и ориентирована на падение сборки при нарушениях.

Различия в том, что ответ ChatGPT делал акцент на гарантированную работоспособность (проверка через намеренное нарушение, отчёты HTML, причины типичных ошибок), а ответ Яндекса — на формально-документальный стиль с перечислением параметров (maxWarnings, maxErrors) и дополнительной интеграцией в check.

Я выбрал подход из ответа ChatGPT, потому что он практичнее: он быстрее приводит к состоянию «checkstyleMain точно работает и падает», а не просто описывает корректную конфигурацию на бумаге.

#### Промпт 2: Проверка кода

**Код для проверки:**

Класс StudentList.java

**Что проверить:**
1. Логические ошибки
2. Соответствие Code Conventions
3. Производительность

**AI-1 (ChatGPT):**

Ниже разбор по твоим 3 пунктам — **логика**, **Code Conventions**, **производительность** — с конкретными замечаниями именно к этому коду.

---

## 1) Логические ошибки / поведение

### ❗ Дубликаты имён ломают консистентность

* Ты ведёшь **и список**, и **мапу по имени**, но:

    * `studentList` может содержать **несколько студентов с одинаковым именем** (ты это не запрещаешь).
    * `studentByName.put(student.name(), student)` перезапишет значение — в мапе останется **последний**, а в списке будут **все**.
* Итог: данные расходятся, а это уже логическая ошибка модели.

**Что сделать:**

* либо запретить дубли по имени,
* либо хранить `Map<String, List<Student>>`,
* либо вообще не держать мапу, если не используешь.

### ❗ MAX_SIZE объявлен, но не используется

Если “максимум 100” — это правило, то его нарушение сейчас никак не предотвращается.

### ❗ studentAdd: проверка имени слишком слабая

Сейчас допускаются имена вроде `"   "` (пробелы) и `" \n"`.
Обычно ожидается `!name.isBlank()` вместо `length() > 0`.

### ❗ findStudentByName делает линейный поиск, игнорируя мапу

Это не логическая ошибка “в смысле неверно”, но это **ошибка дизайна**: ты уже поддерживаешь индекс `studentByName`, но не используешь его.

---

## 2) Соответствие Code Conventions (Java)

### ⚠️ Нейминг метода

`studentAdd` — нетипично для Java. По конвенции: **глагол + объект**

* лучше: `addStudent(Student student)` или просто `add(Student student)`.

### ⚠️ Проверка строк

* `student.name().length() > 0` → предпочтительно `!student.name().isEmpty()` или лучше `!student.name().isBlank()` (Java 11+).

### ⚠️ Сравнение строк

* `student.city() != null && student.city().equals(city)` — норм, но безопаснее/чище:

    * `Objects.equals(student.city(), city)` (и тогда null-check не нужен),
    * или если `city` гарантированно не null: `city.equals(student.city())`.

### ⚠️ Документация vs реализация

* Javadoc у `studentAdd` говорит “если имя не пустое”, но по факту ещё нужна проверка `student != null`, `name != null`. Лучше описать правило полностью.

---

## 3) Производительность

### 🚀 findStudentByName сейчас O(n), можно O(1)

Если ты уже ведёшь `studentByName`, то поиск по имени должен быть:

* `return studentByName.get(name);`

Это сразу делает метод быстрее на больших списках.

### 🚀 getStudentsFromCity — O(n) каждый раз

Для списка до 100 — вообще не проблема, но если ты реально хочешь “по производительности”, то нужен индекс:

* `Map<String, List<Student>> studentsByCity`.

### ⚠️ Потенциальная деградация из-за stream + toList()

`stream().filter().toList()` — ок, но:

* `toList()` возвращает **неизменяемый** список (Java 16+). Это может быть неожиданно для пользователя метода.
* Если нужен изменяемый — `collect(Collectors.toList())`.

---

## Что бы я “починил” минимально, без усложнения

1. Реально использовать `MAX_SIZE`
2. Сделать корректный нейминг
3. Использовать мапу для поиска
4. Решить вопрос с дубликатами (или запретить, или явно задокументировать)

Пример минимально улучшенного варианта (без усложнения индексами по городу):

```java
public void addStudent(Student student) {
  if (student == null) return;

  String name = student.name();
  if (name == null || name.isBlank()) return;

  if (studentList.size() >= MAX_SIZE) return; // или throw IllegalStateException

  // если имена должны быть уникальны:
  if (studentByName.containsKey(name)) return; // или заменить существующего

  studentList.add(student);
  studentByName.put(name, student);
}

public Student findStudentByName(String name) {
  if (name == null || name.isBlank()) return null;
  return studentByName.get(name);
}

public List<Student> getStudentsFromCity(String city) {
  return studentList.stream()
      .filter(student -> java.util.Objects.equals(student.city(), city))
      .toList();
}
```

---

Если скажешь **какое бизнес-правило по имени** (имя уникально или нет?) и что делать при переполнении `MAX_SIZE` (молча игнорировать / исключение / вытеснение) — я дам “идеальный” вариант под твою логику.


**AI-2 (Яндекс):**

Проведу комплексный анализ класса `StudentList.java` по трём направлениям.

## 1. Логические ошибки

Выявлены следующие проблемы:

* **Дублирование поиска по имени**  
  В классе есть два механизма поиска по имени:
    - через `HashMap` (`studentByName`) — эффективный O(1);
    - через перебор списка (`findStudentByName`) — неэффективный O(n).  
      **Проблема:** метод `findStudentByName` игнорирует `studentByName`, что делает HashMap бесполезным.

* **Отсутствие проверки на MAX_SIZE**  
  Константа `MAX_SIZE` объявлена, но нигде не используется. Нет ограничения на количество студентов.

* **Нет обработки коллизий имён**  
  При добавлении студента с уже существующим именем:
    - в `studentList` он добавится (корректно);
    - в `studentByName` старое значение будет **затёрто** (ошибка).  
      **Последствие:** потеря данных при совпадении имён.

* **Неполная валидация при добавлении**  
  Проверяется только `name`, но не `city` или другие поля `Student`.

## 2. Соответствие Code Conventions

**Нарушения Java Code Conventions:**

* **Имена методов**  
  `studentAdd` → должно быть `addStudent` (глагол в начале, camelCase).  
  Остальные методы соответствуют conventions.

* **Документация Javadoc**
    - Нет описания для полей класса (`studentList`, `studentByName`).
    - В `getStudentsFromCity` не указано, что возвращается пустой список при отсутствии совпадений.

* **Видимость полей**  
  Поля `studentList` и `studentByName` — `private final`, что корректно.

* **Комментарии**  
  Избыточные комментарии типа *«Создает пустой список студентов»* для конструктора без логики.

**Рекомендации по улучшению:**
```java
/**
 * Список студентов с возможностью поиска по имени и городу.
 * Ограничение: не более {@value MAX_SIZE} студентов.
 */
public class StudentList {

    /** Максимальное количество студентов в списке. */
    public static final int MAX_SIZE = 100;

    private final List<Student> students = new ArrayList<>();
    private final Map<String, Student> byName = new HashMap<>();

    // Конструктор без комментариев (тривиальный)
    public StudentList() {}

    /**
     * Добавляет студента, если его имя непустое и не превышает лимит.
     * @param student студент для добавления (не null)
     * @return true, если добавление успешно, иначе false
     */
    public boolean addStudent(Student student) {  }
}
```

## 3. Производительность

**Критические проблемы:**

1. **O(n) поиск по имени**  
   Метод `findStudentByName` перебирает весь список. При наличии `HashMap` это недопустимо.  
   **Решение:** заменить на `return studentByName.get(name);`.

2. **Повторный поиск в `getStudentsFromCity`**  
   Метод проходит по всему списку, даже если город не встречается.  
   **Оптимизация:** добавить `Map<String, List<Student>> byCity` для O(1) доступа.

3. **Избыточное создание списков**  
   `getStudentsFromCity` создаёт новый список при каждом вызове.  
   **Альтернатива:** кэшировать результаты для неизменных данных.

4. **Нет ленивой инициализации**  
   Коллекции создаются в конструкторе, даже если не используются.

**Оптимизированная версия ключевых методов:**
```java
public Student findStudentByName(String name) {
    return studentByName.get(name); // O(1) вместо O(n)
}

public List<Student> getStudentsFromCity(String city) {
    return studentList.stream()
        .filter(s -> Objects.equals(s.city(), city))
        .collect(Collectors.toCollection(ArrayList::new));
}
```

**Итоговые рекомендации:**
1. Заменить `findStudentByName` на использование `HashMap`.
2. Добавить проверку `MAX_SIZE` в `addStudent`.
3. Реализовать обработку коллизий имён (например, хранить `List<Student>` в `HashMap`).
4. Переименовать `studentAdd` → `addStudent`.
5. Добавить валидацию всех полей `Student` при добавлении.
6. Рассмотреть добавление `Map<String, List<Student>> byCity` для ускорения `getStudentsFromCity`.

**Сравнение:**

Оба ответа совпали по ключевым проблемам: findStudentByName делает линейный поиск, хотя уже есть studentByName, MAX_SIZE объявлен, но не применяется, и при совпадении имён HashMap затирает старое значение, что ведёт к рассинхрону со списком. Различия в акцентах: ChatGPT дополнительно отметил слабую проверку имени (length()>0 vs isBlank()), возможную неожиданность toList() (неизменяемый список) и предложил минимальный “без усложнения” патч; Яндекс сильнее ушёл в формализм (замечания про «избыточные комментарии», предложения кэшировать/лениво инициализировать коллекции), часть из чего для MAX_SIZE=100 спорна по практической пользе. Я выбрал подход ChatGPT как базовый, потому что он быстрее приводит к критерию успеха “работает корректно и предсказуемо” (уникальность имён/лимит/валидность строк/использование мапы) без преждевременной оптимизации (кэширование, lazy init). При этом идеи Яндекса про byCity я бы добавлял только если точно ожидаются большие объёмы данных или частые запросы по городу.

## Личный глоссарий терминов Dev Tools

### Структура глоссария

Каждый термин содержит:
- **RU / EN** — русское и английское название
- **Определение** — краткое (1–2 предложения) объяснение термина
- **Контекст использования** — где и когда применяется
- **Пример** — конкретное применение в коде/команде/документации
- **Источник** — ссылка на официальную документацию

---

## Категория: Java-экосистема

### JDK — Java Development Kit

**RU / EN:** JDK / Java Development Kit

**Определение:** Набор инструментов для разработки Java-приложений, включающий компилятор javac, JVM, стандартные библиотеки и утилиты.

**Контекст использования:** Используется для компиляции исходного Java-кода и сборки приложений. Обязателен для разработки.

**Пример:** В проекте используется JDK 25, указанный в Gradle Toolchain:

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(25)
        }
    }

**Источник:** https://docs.oracle.com/en/java/javase/21/docs/

---

### JRE — Java Runtime Environment

**RU / EN:** JRE / Java Runtime Environment

**Определение:** Среда выполнения Java-приложений, включающая JVM и стандартные библиотеки, но не содержащая инструментов разработки.

**Контекст использования:** Используется для запуска уже скомпилированных Java-программ.

**Пример:** Запуск приложения командой `java -jar app.jar`.

**Источник:** https://docs.oracle.com/en/java/javase/21/docs/specs/man/java.html

---

### JVM — Java Virtual Machine

**RU / EN:** JVM / Java Virtual Machine

**Определение:** Виртуальная машина, исполняющая Java-байт-код и обеспечивающая независимость Java-программ от платформы.

**Контекст использования:** Запускает class-файлы, управляет памятью и сборкой мусора.

**Пример:** После компиляции `javac Main.java` выполнение происходит через `java Main`.

**Источник:** https://docs.oracle.com/javase/specs/jvms/se21/html/

---

### Gradle Wrapper — Gradle Wrapper

**RU / EN:** Gradle Wrapper / Gradle Wrapper

**Определение:** Набор скриптов для запуска проекта с фиксированной версией Gradle без необходимости глобальной установки.

**Контекст использования:** Гарантирует воспроизводимую сборку проекта на разных машинах.

**Пример:** Запуск тестов командой `./gradlew test`.

**Источник:** https://docs.gradle.org/current/userguide/gradle_wrapper.html

---

### Dependency — Зависимость

**RU / EN:** Dependency / Dependency

**Определение:** Внешняя библиотека или модуль, от которого зависит проект.

**Контекст использования:** Управляется системой сборки (Gradle).

**Пример:**

    testImplementation "org.junit.jupiter:junit-jupiter"

**Источник:** https://docs.gradle.org/current/userguide/dependency_management.html

---

### Artifact — Артефакт сборки

**RU / EN:** Artifact / Artifact

**Определение:** Результат сборки проекта (например, JAR-файл), готовый к запуску или распространению.

**Контекст использования:** Используется для деплоя или передачи приложения.

**Пример:** devtools-1.0-SNAPSHOT.jar.

**Источник:** https://maven.apache.org/pom.html#Artifacts

---

## Категория: Инструменты разработки

### IDE — Integrated Development Environment

**RU / EN:** IDE / Integrated Development Environment

**Определение:** Среда разработки, объединяющая редактор кода, отладчик и инструменты сборки.

**Контекст использования:** Упрощает разработку и отладку приложений.

**Пример:** IntelliJ IDEA используется для проекта devtools.

**Источник:** https://www.jetbrains.com/idea/documentation/

---

### Git — Git

**RU / EN:** Git / Git

**Определение:** Распределённая система контроля версий для отслеживания изменений в коде.

**Контекст использования:** Управление историей проекта и командная работа.

**Пример:** Создание ветки feature/DVT-12.

**Источник:** https://git-scm.com/docs/git

---

### Repository — Репозиторий

**RU / EN:** Repository / Repository

**Определение:** Хранилище исходного кода и истории изменений.

**Контекст использования:** Содержит весь проект devtools.

**Пример:** Репозиторий на GitHub.

**Источник:** https://git-scm.com/docs/gitglossary#def_repository

---

### Commit — Коммит

**RU / EN:** Commit / Commit

**Определение:** Зафиксированное состояние проекта с описанием внесённых изменений.

**Контекст использования:** Сохранение логически завершённых изменений.

**Пример:** `git commit -m "Add Dev Tools glossary (DVT-12)"`.

**Источник:** https://git-scm.com/docs/git-commit

---

### Branch — Ветка

**RU / EN:** Branch / Branch

**Определение:** Независимая линия разработки внутри репозитория.

**Контекст использования:** Разработка фич без влияния на основную ветку.

**Пример:** Ветка feature/DVT-12.

**Источник:** https://git-scm.com/docs/git-branch

---

### Pull Request — Pull Request

**RU / EN:** Pull Request / Pull Request

**Определение:** Запрос на слияние изменений с возможностью code review и запуска CI.

**Контекст использования:** Проверка качества кода перед merge.

**Пример:** Pull Request из feature/DVT-12 в master.

**Источник:** https://docs.github.com/en/pull-requests

---

### Checkstyle — Checkstyle

**RU / EN:** Checkstyle / Checkstyle

**Определение:** Инструмент статического анализа Java-кода для проверки код-стайла.

**Контекст использования:** Используется в CI для автоматической проверки.

**Пример:** Задача Gradle `checkstyleMain`.

**Источник:** https://checkstyle.sourceforge.io/

---

### Debug — Отладка

**RU / EN:** Debug / Debug

**Определение:** Процесс поиска и исправления ошибок в программе.

**Контекст использования:** Анализ поведения приложения во время выполнения.

**Пример:** Запуск приложения в Debug-режиме в IntelliJ IDEA.

**Источник:** https://www.jetbrains.com/help/idea/debugging-code.html

---

### Breakpoint — Точка останова

**RU / EN:** Breakpoint / Breakpoint

**Определение:** Место в коде, где выполнение программы приостанавливается при отладке.

**Контекст использования:** Позволяет анализировать состояние программы.

**Пример:** Breakpoint в методе main().

**Источник:** https://www.jetbrains.com/help/idea/using-breakpoints.html

---

## Категория: Процессы и практики

### Code Review — Code Review

**RU / EN:** Code Review / Code Review

**Определение:** Процесс проверки кода другими разработчиками перед его слиянием.

**Контекст использования:** Повышает качество кода и снижает количество ошибок.

**Пример:** Ревью Pull Request DVT-12.

**Источник:** https://martinfowler.com/articles/code-review.html

---

### CI/CD — Continuous Integration / Continuous Delivery

**RU / EN:** CI/CD / Continuous Integration, Continuous Delivery

**Определение:** Практика автоматической сборки, тестирования и доставки кода.

**Контекст использования:** Автоматическая проверка Pull Request.

**Пример:** CI запускает тесты и Checkstyle при каждом PR.

**Источник:** https://www.atlassian.com/continuous-delivery

---

### Runbook — Runbook

**RU / EN:** Runbook / Runbook

**Определение:** Документ с инструкциями по эксплуатации и устранению инцидентов.

**Контекст использования:** Поддержка и сопровождение системы.

**Пример:** Runbook с шагами перезапуска сервиса.

**Источник:** https://sre.google/sre-book/monitoring-distributed-systems/

---

## Вопросы по сложным терминам

### Вопрос 1: JDK / JRE / JVM

**Задача:** Понять различия между JDK, JRE и JVM.

**Контекст:** Изучил документацию Oracle и использую JDK 25 в проекте.

**Ограничения:** Большинство материалов ориентированы на Java 8.

**Ожидаемый результат:** Чёткое понимание ролей каждого компонента.

**Критерии успеха:** Могу объяснить разницу и привести практический пример.

---

### Вопрос 2: Pull Request

**Задача:** Понять назначение Pull Request и его роль в командной разработке.

**Контекст:** Создаю Pull Request для feature/DVT-12.

**Ограничения:** Ранее работал только с прямыми коммитами в master.

**Ожидаемый результат:** Понимание процесса ревью и CI.

**Критерии успеха:** Умею создавать Pull Request с описанием и успешно проходить ревью.
